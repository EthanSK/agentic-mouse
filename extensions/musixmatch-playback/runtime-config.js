// Deliberately inert until the physical button, transport and live semantic
// control have all been proven. Replace these nulls only with recorded evidence.
globalThis.AgenticMouseMusixmatchConfig = Object.freeze({
  enabled: false,
  transport: null,
  target: null
});
