# Agentic Mouse project rules

- This repository owns the source/build architecture for shared named Karabiner actions across Ethan's Corsair Scimitar and left-handed Razer Naga, plus the Scimitar's explicit classic phone multi-tap mode and temporary runtime indicator.
- Preserve the existing working iCUE assignments, 2,750 DPI, Logitech setup, Karabiner rules, VoiceInk++ route, and all unrelated hardware and application settings.
- Utility Zoom follows the standard computer wheel convention on both mice: physical wheel up means Zoom In and physical wheel down means Zoom Out. This current Zoom-only rule overrides the older combined-polarity sentence below; do not flip Brightness, Spaces, Horizontal Scroll, Clipboard, YouTube, or another wheel family with it. (Codex task: 01a039f7-873c-7c30-b3dc-af8a6724ace5)
- The authoritative global mapping is Ethan's latest shared physical-cell map: wheel click = Play/Pause through an exact-device Karabiner rule on both mice; hold physical cell 1 and move the ratcheted wheel to scroll horizontally by the bounded configurable `input.horizontalScrollLinesPerRatchet` magnitude (default 4 native line units) per accepted detent; hold physical cell 4 and move it for Copy / Paste; cell 2 opens the current frontmost app's mode and exits that app-specific child when pressed again; cell 3 single-presses selected-area Screenshot, cancels it while its crosshair is active, and rapid-double-pastes the last screenshot Agentic Mouse copied; cell 5 = ordinary Forward with a VS Code Previous Change override; click physical cell 6 without wheel input to rewind the selected YouTube target by exactly five seconds, or hold it and move the ratcheted wheel to scrub by exactly five seconds per reconstructed detent through the existing VoiceInk bridge without focusing Chrome; cell 7 = Enter outside runtime modes; cell 8 = ordinary Back with a VS Code Next Change override; cell 9 opens Keys mode; cell 10 toggles that mouse's persistent Default legend outside modes and is the universal Exit while any mode is active; cell 11 holds open Switch App; cell 12 opens Utility immediately; and the top DPI control triggers VoiceInk++ speech-to-text without changing the fixed 2,750 DPI. Mirror every semantic action through the canonical physical-cell crosswalk: horizontal-wheel cell 1 is Corsair printed 1 / Razer printed 3; Copy/Paste-wheel cell 4 is Corsair printed 4 / Razer printed 6; app-mode entry / child Exit cell 2 is Corsair printed 2 / Razer printed 2; screenshot cell 3 is Corsair printed 3 / Razer printed 1; YouTube Scrub + Wheel cell 6 is Corsair printed 6 / Razer printed 4; Enter cell 7 is Corsair printed 7 / Razer printed 9; Keys cell 9 is Corsair printed 9 / Razer printed 7; Legend toggle / universal active Exit cell 10 is Corsair printed 10 / Razer printed 12; Switch App cell 11 is Corsair printed 11 / Razer printed 11; and Utility cell 12 is Corsair printed 12 / Razer printed 10. One unfiltered base rule per mouse applies in every application, with only explicit app overrides and matching base exclusions. A runtime mode may intercept these keys only while explicitly active and must restore the global mapping on exit. (Codex task: 01a039f7-873c-7c30-b3dc-af8a6724ace5)
- Never edit iCUE's private database/profile files directly. Live iCUE changes must be performed visibly and verified through the app.
- Keep exactly one canonical iCUE software profile for the Scimitar. It owns DPI, lighting, and the neutral side-grid/DPI transports for every application. Never create or duplicate app-linked iCUE profiles for semantic behavior; app-specific overrides belong in exact-device Karabiner conditions or Agentic Mouse modes. Extra software profiles are a regression because an empty profile can silently remove every neutral transport.
- Treat an inactive or locked macOS session as a hard security boundary. Agentic Mouse must renew only a short absolute-expiry unlocked-session lease from documented `NSWorkspace` session notifications; every generated command must require that lease both when its manipulator matches and when delayed/release output fires. When the lease is absent, expired, or explicitly cleared, an exact-device sink must consume all Corsair and Razer neutral side-grid, DPI, and custom wheel transports so they cannot leak into the lock screen. Lock, fast-user-switch, screen sleep, app failure, and lease-write failure must tear down modes, HUDs, pending text, and synthetic actions immediately. Unlock may restore only the idle runtime baseline, never a prior mode, HUD, pending action, or queued key release. Preserve ordinary pointer motion, scrolling, and primary/secondary clicks as normal macOS input.
- Package every build intended to replace Ethan's `/Applications/AgenticMouse.app` with the stable `Developer ID Application: Ethan Sarif-Kattan (T34G959ZG8)` identity via `CODE_SIGN_IDENTITY`; never install an ad-hoc replacement. An ad-hoc designated requirement is the changing code hash and silently invalidates the app's Accessibility grant on every rebuild. Never reset TCC or invoke `sfltool` for this workflow; verify the signed requirement, let Ethan grant the stable app once in System Settings, and preserve that identity/path on future installs.
- **A USER-REQUESTED AGENTIC MOUSE CHANGE IS NOT COMPLETE UNTIL IT IS LIVE.** After the requested source change passes the full gate, automatically package one stable Developer-ID candidate, advance the real marketing version and build number exactly once, preserve the current installed bundle as a recoverable rollback, send one native macOS heads-up, record which source-specific Default legends are visible, send the app's idempotent source-specific close command to both mouse coordinators before replacing the selected Karabiner block even when no mode lease remains, install only `Karabiner/generated/agentic-mouse-runtime.json`, cleanly terminate only the exact running Agentic Mouse app through its dedicated `Quit Agentic Mouse` menu item, replace `/Applications/AgenticMouse.app`, relaunch that exact path, and read back the installed signature, version, executable hash, embedded iCUE SDK hash, running PID, command-socket ownership, and relevant live-config/Karabiner preservation. After the active-session input gate is ready, restore exactly the Default legends that were visible before the attended replacement and verify one HUD per connected display; restarting the app clears this in-memory presentation state, so zero HUD windows is not a correct handoff when Ethan had left a legend open. Never install the five-rule `agentic-mouse.json` base export: it can still open modes but omits the active-mode routing and universal Exit rule. A generic AppleEvent Quit bypasses the dedicated supervisor-disarm path, and reloading Karabiner while a runtime mode is active resets the mode variables before the HUD receives Exit; a missing lease therefore does not prove that every HUD closed. Never use either unsafe sequence during installation. Do not stop at a green source candidate, ask for a second install approval, or leave Ethan running the previous build merely because installation is a separate script step. A request to change or fix this app authorizes this normal signed install/relaunch completion step. Stop before installation only when Ethan explicitly says source-only/no-install/not-now, when a real safety or ownership conflict remains, or when an external user-only permission blocks completion; report that exact boundary immediately. Automated checks and a successful install still do not replace any genuinely physical behavior acceptance.
- Keep persistent runtime recovery on the supported signed nested `SMAppService.loginItem` helper packaged at `Contents/Library/LoginItems/AgenticMouseSupervisor.app`. It may relaunch an unexpectedly absent main app through non-activating `NSWorkspace` with bounded backoff, crash-loop protection, and the per-user main-process instance lock. Defer relaunch while loginwindow or the screensaver owns the session. Every launch, display wake, or transition away from loginwindow must remain fail closed until a public global event monitor proves real input while a normal app is frontmost; loginwindow activation must clear the lease immediately. Explicit Quit must use Service Management's asynchronous unregister completion so the running supervisor is killed before main-app termination, and must cancel Quit if disarming recovery fails; a normal unexpected `SIGTERM` remains recoverable. Do not replace this design with `SMAppService.mainApp`, a LaunchAgent, private service files, process-name polling, a marker-file convention, a private lock-state key, or a relaunch path outside the exact containing signed app. Preserve locked-session teardown across every restart.
- Keep semantic actions separate from physical bindings. Agentic Mouse may generate Karabiner-compatible output, but no Corsair or Razer position enters the binding file or live Karabiner profile until Ethan approves that exact physical-cell choice and the exact-device source event has been captured.
- Treat Ethan naming a `Corsair n` or `Razer n` button as a reference to that button's canonical physical cell, not as permission to create device-specific semantics. He may switch mice and name whichever printed label is under his thumb. Resolve the named label through the crosswalk and apply the requested semantic change to both mice. Diverge only when Ethan explicitly asks the mice to behave differently for a stated hardware, handedness, transport, lighting, or presentation reason; merely naming one mouse is never enough. Preserve transport key codes, printed HUD labels, independent HUD corners, and lighting calibration. The one-cell top-level Copy/Paste and Horizontal Scroll wheel chords use the same accepted wheel polarity on both mice; only Keys arrows retain the explicit left-handed Razer horizontal projection.
- Preserve layered ownership: iCUE owns Corsair hardware settings and neutral transports; the Naga onboard profile owns its hardware transports; enabled exact-device mappings live in Karabiner; Agentic Mouse owns semantic source files, generation, and explicitly approved runtime modes. Never store a live profile, private serial, or second hidden mapping source here.
- Philips Hue-to-mouse mirroring is retired. Do not restore its runtime, configuration, network access, or UI.
- Target only the exact Scimitar device discovered at runtime. Never apply SDK lighting to all Corsair devices.
- Use shared iCUE SDK lighting control. Do not request exclusive lighting control. Runtime input comes from exact-device Karabiner `send_user_command` press/release phases; do not add a parallel raw iCUE semantic-input source. Keep the older `CAL_ExclusiveKeyEventsListening` transport source-only unless a separately approved diagnostic requires it.
- Multi-tap mode must be visibly and behaviorally modal, must not activate or focus its reference HUD, and must restore the normal mouse behavior immediately on exit, disconnect, or process failure.
- The Default mode legend is a source-specific one-press toggle on canonical physical cell 10 (Corsair printed 10 / Razer printed 12). Each mouse owns an independent legend: repeating that mouse's press hides only its copies, while the other mouse's legend may remain visible in parallel. Corsair legends sit bottom-right; left-handed Razer legends sit bottom-left. Each shows one non-activating panel on every connected display, requires no Accessibility permission, takes no Karabiner mode lease, and never changes mouse lighting or ordinary mappings. Keep user-facing copy concrete (`Default mode`, `Legend toggle`); never expose internal terms such as `passive reminder` as HUD labels. Screenshot copy is stateful and truthful: use `Screenshot` before a capture, `Cancel screenshot` while the crosshair is active, `Copying screenshot…` while the native save is pending, and `Screenshot · 2× Paste` while Agentic Mouse still owns the current screenshot clipboard item. Never guess when state belongs to an external app Agentic Mouse cannot observe reliably.
- Physical cell 3 (Corsair printed 3 / Razer printed 1) owns selected-area Screenshot outside modes. A single press must emit the exact native Shift-Command-4 key cycle after a bounded 280 ms same-source double-press window so macOS owns the configured save destination, sound, and bottom-right floating thumbnail. Track only the bounded interaction Agentic Mouse started with a short-lived global mouse-up/Escape monitor; completion or Escape clears that session, and a press while the crosshair remains active sends Escape to cancel it. After completion, watch only the configured Screenshot folder for a new image absent from the capture-start snapshot, prefer the file carrying macOS screenshot metadata, fail closed on ambiguity, and copy that exact saved image to the pasteboard. A rapid same-source double press while that owned pasteboard item remains current sends one hardware-shaped Command-V; if the clipboard changed, never restore or paste the stale image. A double press while native saving is pending may queue only that paste until the bounded file copy succeeds. Do not invoke `screencapture` directly, request Screen & System Audio Recording merely to save a screenshot, take a second capture, represent state with an unbounded timer, leave a stale latch, or quit/restart/toggle VoiceInk. Lock, sleep, reload, and app shutdown must cancel the classifier, watcher, deferred paste, and exact interaction.
- Physical cell 12 (Corsair printed 12 / Razer printed 10) opens Utility modes immediately. The first Utility page lists only implemented utility actions: physical cell 3 is Spaces + Wheel, physical cell 4 is Mission Control / Show Desktop + Wheel, physical cell 5 is App Exposé + Wheel, physical cell 6 is Magnet + Wheel, cell 7 types the optional device-local Keychain password, cell 8 opens Intelligence on Demand with one hardware-shaped Option-Space lifecycle, cell 11 opens the lower-priority manual configured-app selector, and cell 12 opens the nested Extra Utilities page. Extra Utilities uses physical cell 1 (Corsair printed 1 / Razer printed 3) for the manual `Organize Windows` action, which emits exactly one hardware-like Control-Option-Shift-Command-A lifecycle to Stay's saved `Agentic Mouse Layout v1`; Stay records physical key code 0 for that reserved shortcut. Extra Utilities physical cell 9 (Corsair printed 9 / Razer printed 7) sends one normal Command-Q to the actual frontmost external application and is latched once per Extra Utilities visit so repeated presses cannot quit a chain of newly frontmost apps. Never force quit, target either Agentic Mouse process, or bypass the destination app's save-confirmation dialog. Utility and Extra Utilities actions are app-owned outputs; reject an unexpected `selectNative` command without setting a one-shot latch or claiming success. Never restore windows automatically or drive Stay through AppleScript/UI automation. Do not revert to Quartz F14: live testing proved it reaches the event stream but does not activate Stay's global hotkey. Render the cell-7 action title as exactly `PP` in every legend and public map; never spell out its credential purpose on a casual mapping surface. Keypad is a child of Keys mode on physical cell 6, never a Utility entry. Every active mode exits through universal physical cell 10 (Corsair printed 10 / mirrored Razer printed 12); every app-specific child additionally exits through its matching top-level entry cell 2. The manual Choose App selector itself still reserves only cell 10 because cell 2 chooses Terminal there. Outside modes, cell 10 toggles the source mouse's persistent Default legend. Physical cell 11 holds open Switch App outside modes. App-specific children keep cell 12 available for a real app action; VS Code, Terminal, and iTerm use it for one app-targeted Control-C terminal interrupt, while unrelated app modes leave it Spare. They never reserve cell 12 as a second back or exit control. Colour Proof has completed validation and must not appear as a generated rule or selectable slot; keep its proven controller source only as internal regression infrastructure. Future modes stay in the outstanding ledger until their trigger, action map, colour, and exit are wired.
- On the first Utility page, hold physical cell 1 and move the ratcheted wheel for Brightness, hold cell 2 for Zoom, hold cell 3 for Spaces, hold cell 4 for Mission Control / Show Desktop, hold cell 5 and ratchet down for App Exposé, and hold cell 6 for Magnet Left / Right. Wheel up means Decrease / Zoom Out / Space Right / Mission Control / Magnet Left; wheel down means Increase / Zoom In / Space Left / Show Desktop / App Exposé / Magnet Right. Utility cell 5 deliberately ignores and consumes wheel-up. Its first wheel-down per physical hold posts this Mac's enabled Application Windows shortcut, Control-Fn-Down, and later ratchets remain consumed until release so the system toggle cannot immediately close again. Never restore the superseded Command-` / Shift-Command-` window-cycle behavior, use Accessibility/Dock UI automation, or infer the shortcut instead of reading symbolic hotkey 33. Magnet owns placement through its configured global Control-Option-Left/Right accelerators. After returning from the wheel callback, use one long-lived HID event source with local-event suppression disabled and post the complete hardware-like lifecycle with real `flagsChanged` modifier transitions: Control down, Option down, Arrow down/up carrying Control+Option+SecondaryFn+NumericPad, Option up, Control up. Pre-create the complete sequence, give each phase a strictly increasing timestamp and bounded dwell, and never post synthetic input from inside the Quartz wheel callback. Omitting the physical arrow flags leaks `^[[1;7C` / `^[[1;7D` into a terminal and does not reproduce Magnet's accelerator. Reconstruct one Magnet detent by coalescing same-direction raw-event bursts within 150 ms while accepting an immediate direction reversal; serialize reconstructed detents FIFO with a 400 ms minimum interval between shortcut lifecycles, cap the pending queue at three, and cancel pending requests on physical release, lock, sleep, mode teardown, source loss, reload, or shutdown. Never open Magnet's menu-bar UI, invoke its `AXMenuItem` commands, use a root-only VirtualHID helper, edit Magnet's private state, or claim cross-monitor acceptance before a physical multi-display test. Top-level physical cell 4 owns Copy/Paste directly, independent of Utility page and lease state. Ethan physically rejected the initial Quartz-sign polarity: wheel up means Space Right and wheel down means Space Left. Top-level Horizontal Scroll and YouTube Scrub follow the same physical convention: wheel up moves right/forward and wheel down moves left/back. `WheelChordDirection` is the normalized Quartz sign rather than the physical ratchet; for YouTube Scrub on both accepted mice, physical up reaches the resolver as `.down`, so map `.down` to forward and `.up` to backward. Keep that inversion local and never infer this control's physical direction from the enum label. Read direction from line delta first, then point and fixed-point fallbacks; consume phase-free wheel input while a chord is held, but pass through any trackpad or smooth-scroll event carrying scroll or momentum phase. Brightness and Zoom remain continuous per accepted event. Horizontal Scroll, YouTube Scrub, Copy/Paste, and Chrome Tabs coalesce same-direction raw bursts into one discrete ratchet while accepting an immediate direction reversal. Each accepted YouTube Scrub ratchet emits exactly one strict no-payload distributed notification: forward uses `com.ethansk.agenticmouse.youtube.seekForwardFiveSeconds` and backward uses `com.ethansk.agenticmouse.youtube.seekBackwardFiveSeconds`; the VoiceInk bridge must accept only exact `+5` and `-5`, preserve play state, clamp within the media duration, retain its existing target-selection order, and never focus Chrome. Mission Control / Show Desktop, App Exposé, and Spaces act at most once per physical hold; a duplicate press command must not re-arm them. This Mac's enabled Space shortcuts are Control-Fn-Left and Control-Fn-Right. Post one complete Control `flagsChanged`, Arrow down/up with Control+SecondaryFn+NumericPad, Control `flagsChanged`-up lifecycle only after returning from the Quartz event-tap callback; never use a flag-only Control-Arrow pair or post a partial modifier lifecycle. Keep the temporary per-source wheel trace diagnostic-only: record the armed cell, cumulative accepted-event count, raw line/point/fixed deltas, phase flags, routing result, shortcut post, and observed active-Space notification. It may update the footer only when that source's persistent Default legend is already visible; otherwise keep the trace in logs and never open a panel or inject it into an active mode HUD. After each held-wheel route actually accepts or rejects its output, replace the owning visible HUD's bottom-left footer with concise action-specific feedback and the cumulative ratchet count; use informational `sent` wording for dispatch-only evidence, a confirmed tone only for an observed destination state, and a not-confirmed tone for rejection or missing observation. Active Utility and app-specific modes use their own source HUD. Top-level Copy/Paste, Horizontal Scroll, and YouTube Scrub may update only that source's already-visible Default legend and must never auto-show it. Reset the counter on every real press/release lifecycle; later events may advance the diagnostic count while a one-action hold latch or duplicate-burst filter suppresses further shortcuts. Cell 7 types the optional device-local Keychain password through the same lock and Accessibility gates as other synthetic input, cell 8 opens Intelligence on Demand through one complete Option-Space lifecycle, cell 9 enters Keys mode, and cell 11 opens the manual configured-app selector. Exact-device Karabiner press/release establishes which mouse owns the chord; the Accessibility-trusted Agentic Mouse Quartz event tap owns wheel conversion and the bounded system-shortcut actions because Karabiner cannot use wheel movement as a basic manipulator input. If both mice hold a chord, consume without guessing. On lock, sleep, device loss, lease failure, app shutdown, or configuration reload, clear every chord and pending diagnostic. Never let top-level YouTube Scrub leak into Keypad or app-specific child modes.
- Both Razer DPI buttons keep their onboard neutral transports (upper F21 and lower F22), and the Corsair top DPI button uses its named iCUE F19 neutral transport. All three exact-device Karabiner adapters trigger the same VoiceInk++ primary shortcut only on source release. Karabiner exclusively owns these keyboard interfaces and collapses the transports into one normalized Primary chord, so VoiceInk++ cannot recover F19/F21/F22 source identity. VoiceInk++ owns a 90 ms complete-chord coalescer before its Primary gesture classifier: the first chord acts immediately, a second chord inside that narrow window is discarded, and later deliberate double/triple gestures remain available. Do not restore the rejected 500 ms blanket cooldown or add a second hardware-specific timer in Karabiner. Do not claim physical release timing for either mouse until EventViewer proves one down at press and one up at physical release; preserve the official iCUE export and the prior generated/live Karabiner backup as rollback.
- Every runtime mode must have a distinct, strongly saturated intentional colour, must exit through universal physical cell 10, and must show the floating legend on every connected display for the whole mode lifetime. Use the current mode colour at full strength for every ordinary card border, the panel identity, and mouse lighting. Ordinary actions keep their action-family colour as a calmer, darkened but fully opaque card fill so twelve controls do not compete with the active mode. Any card that opens another mode or submenu is the deliberate exception: give it the destination mode's exact saturated colour as both its fully opaque fill and its thicker border, so only modes themselves receive the strongest card treatment. Selection remains stronger still through border weight and opacity. Related controls must share one fill; a two-way hold-plus-wheel control is one family card, all four arrow keys are another family, and unrelated controls retain distinct fills. Default mode is the neutral baseline and uses white ordinary card borders, never the blue runtime accent. Never weaken or pastelize the hardware mode colour. Every card title and printed-button label must remain fully white on every fill; never introduce luminance-adaptive black text or dim the printed label through opacity. Every card content stack must keep an explicit 12-point horizontal inset inside its border; grid spacing and a two-line limit do not substitute for internal padding. Do not reserve an in-mode Legend toggle cell; cell 10 exits and closes the mode HUD. Corsair and Razer use separate coordinators, presenters, and Karabiner lease variables so both HUDs and different modes can coexist without changing each other's routing, lighting, or ordinary map. If that mouse's persistent top-level legend was visible before mode entry, restore it after exit; otherwise close its mode HUD on exit. Make the main function the prominent card text and put one small printed button label for the exact source mouse underneath it.
- Every shared HUD must retain canonical physical cells internally and render only the active source mouse's printed labels. Never duplicate or renumber the semantic map to achieve presentation-only numbering.
- Treat the latest explicit physical report as the source of truth for visible control health. If Ethan reports a button broken or unreliable and has not later physically accepted it, set that card's `ModeHUDControlStatus` to `reportedBroken` and render one `❌` immediately after the source-specific printed identifier (`Corsair n ❌` / `Razer n ❌`), never inside the action title. A source edit, automated test, signed install, or merely untested state does not clear the marker; remove it only after a later explicit physical success report. For a combined hold-plus-wheel card, keep the marker while either advertised direction remains broken. Remove retired controls instead of marking them, and never leave a cross on a control Ethan later accepted.
- Match the accepted G502 Better Git gesture in both exact-device VS Code layers: physical cell 5 single-click emits F17 Previous Change and double-click emits F19 Stage + Previous; physical cell 8 single-click emits F13 Next Change and double-click emits F18 Stage + Next. Use a 300 ms window, keep every output non-repeating, and preserve ordinary Forward/Back outside VS Code. Top-level physical cell 6 remains YouTube Scrub + Wheel in VS Code as well as every other app; only the explicitly entered VS Code child mode reuses cell 9 for a combined Stage + Next single / exact Undo double gesture and cell 6 for Cursor History + Wheel. In that child, physical wheel down invokes VS Code's built-in `workbench.action.navigateBack` and physical wheel up invokes `workbench.action.navigateForward` through the bundled, allow-listed Agentic Mouse VS Code Bridge extension. Do not synthesize Cursor History keyboard events, alter Ethan's keybindings, or treat successful event posting as command execution: two physically installed target-PID keyboard transports failed inside Electron even after their full modifier lifecycles were correct. Require VS Code to be running and frontmost, retain the locked-session gate, open only the exact `vscode://ethansk.agentic-mouse-vscode-bridge/cursor-history/back|forward` routes without activating another app, and let the extension call those two built-in commands directly. Cursor History must coalesce every same-direction raw burst through a sliding 150 ms quiet gap: suppressed raw events extend the gap, one ratchet yields exactly one history jump, a deliberate reversal remains immediate, and a distinct ratchet after the quiet gap remains repeatable during the same hold. Preserve the other wheel controls' accepted cadence and polarity. Clear an armed chord if automatic app targeting changes. Better Git v1.2.53+ owns the repository-aware F16 semantics: it observes the latest exact Git-index transition regardless of whether staging came from Better Git, VS Code, another mouse binding, or `git add`, and refuses when `HEAD` or the current index no longer matches. Never duplicate that Git-state inference in Agentic Mouse or Karabiner.
- Draw every 4 × 3 mouse HUD from the top of the mouse toward the desk. The right-handed Corsair uses canonical rows `3/6/9/12`, `2/5/8/11`, then `1/4/7/10`. The left-handed Razer reverses each row for the horizontal mirror: `12/9/6/3`, `11/8/5/2`, then `10/7/4/1`. Keep the `1/4/7/10` physical row at the bottom and change presentation only, never the canonical semantic cells.
- Keypad is selected from Keys mode with physical cell 6. Cell 1 cycles punctuation, cells 2–9 carry the familiar ABC through WXYZ groups (including DEF on physical cell 3), and a long hold on cells 1–9 emits that digit. Universal cell 10 exits; tap cell 11 for Space; tap cell 12 for Backspace and hold cell 12 for Return. The large HUD label must use the actual 1–12 number printed on the source mouse, never the fictional phone `* / 0 / #` bottom row. Preserve the source-specific physical grid so Razer printed 1 appears at its real top-right position. Show every punctuation character in a compact multi-row preview rather than clipping the long cell-1 cycle. Its exact-device press/release stream must remain focus-anchored and fail closed.
- Top-level physical cell 2 opens the current frontmost app's mode directly and refreshes its HUD when the frontmost application changes. Utility cell 11 opens the separate manual selector for the eleven highest-value named targets that fit the grid: Codex, Terminal, Claude, Chrome, iTerm, Spotify, VS Code, Notion, OBS, Telegram, and Safari. Selecting an app locks that target without activating it, and later focus changes must not retarget that manual journey. Automatic frontmost-app detection additionally recognizes Firefox, Opera, Restream Chat++, Preview, Mail, iCUE, Karabiner-Elements, System Settings, Finder, and Karabiner-EventViewer. Press cell 2 or universal cell 10 to exit any selected app child; the parent Choose App selector continues to use cell 2 for Terminal and exits only through cell 10. Deliver ordinary app shortcuts through one generic, bounded `CGEvent.postToPid` adapter selected by bundle identifier; only each app's shortcut meanings are app-specific.
- Spotify mode combines volume into one held-wheel control on canonical physical cell 7 (Corsair printed 7 / Razer printed 9); cell 8 remains honestly Spare. While cell 7 is physically held, each reconstructed wheel-up ratchet sends Spotify's native Command-Up volume increase and wheel-down sends Command-Down volume decrease directly to the running Spotify PID without activating it. Coalesce same-direction raw-event bursts for 80 ms, accept an immediate reversal, clear the chord on release or every existing teardown boundary, and pass ordinary scrolling through whenever the card is not held. Automatic frontmost Spotify mode and manually chosen Spotify mode must resolve this same control from the one shared definition.
- Codex provides Steer Queued Message on physical cell 1 (Corsair printed 1 / Razer printed 3), the duplicate app-child Exit on shared physical cell 2, Pin/Unpin on shared physical cell 3 (Corsair printed 3 / Razer printed 1), Reasoning Effort + Wheel on shared physical cell 4 (Corsair printed 4 / Razer printed 6), New Chat on shared cell 5 (Corsair printed 5 / Razer printed 5), Mute/Unmute Voice Mic on shared physical cell 6 (Corsair printed 6 / Razer printed 4), Enter on shared physical cell 7 (Corsair printed 7 / Razer printed 9), Edit Queued Message on shared physical cell 8, Open in Side Chat on shared physical cell 9 (Corsair printed 9 / Razer printed 7), universal Exit on cell 10, Chats Selection + Wheel on shared physical cell 11, and Voice Mode on shared physical cell 12 (Corsair printed 12 / Razer printed 10). On cell 4, physical wheel up must send the existing Hyper-F18 Reasoning Effort increase binding and physical wheel down must send Hyper-F19 decrease. `WheelChordDirection` names the normalized Quartz delta sign, not the ratchet direction Ethan feels: on both accepted mice, physical wheel up reaches the resolver as `.down`, so map `.down` to increase and `.up` to decrease. Never infer the user-facing direction from that enum label or globally flip another wheel family. On cell 11, wheel up sends Option-Command-Right for the next/forward chat and wheel down sends Option-Command-Left for the previous/back chat. Both wheel controls use the shared app-specific press/release architecture, target the running Codex PID without activation, accept immediate reversal, and report the exact dispatched direction in that mouse's Codex HUD footer. Keep Reasoning Effort's sliding 150 ms same-direction quiet gap so one raw burst produces one semantic step. Chats Selection is deliberately different: use a fixed 80 ms leading-edge duplicate-burst filter so each later physical ratchet advances exactly one chat without waiting for a 150 ms silent gap; suppressed raw duplicates must not extend that filter. Preserve the working Chats Selection transport and direction, which Ethan physically accepted before requesting this cadence change. The redundant broken New Voice Chat card is retired; Voice Mode keeps the direct Control-Shift-V `realtimeVoice` command. Recent Chats is deliberately absent because Ethan physically rejected its unreliable behavior; do not reintroduce the `previousRecentThread` path. The Voice Mic control invokes `realtimeVoice.toggleMicrophoneMute` and is meaningful only while a Codex Voice Mode session is active; keep its HUD copy explicit about that scope. ChatGPT 26.818.61809 registers `realtimeVoice` as an OS-global Electron accelerator; deliver its physical ANSI V key code 9 with a complete hardware-shaped Control-down, Shift-down, V-down/up, Shift-up, Control-up lifecycle while ChatGPT is frontmost. Do not reinterpret that Carbon key registration through Dvorak character translation and do not replace Ethan's normal keyboard shortcut. Before any AX read, require the unlocked-session and Accessibility gates. Observe every non-minimized top-level ChatGPT window with a bounded messaging timeout for exact enabled pressable `Start voice chat` / `Start new voice chat` / `Resume voice chat` / `Open voice chat` versus `Stop voice chat` controls before and after dispatch; report confirmed only from a real state transition and retain the repair marker until Ethan physically accepts the installed build. Cancel all Voice/Pin verification on lock, sleep, app teardown, or when no Codex mode remains active, and never re-show feedback in a source HUD that already left Codex mode. Open in Side Chat uses Codex's built-in Command-Option-S `openSideChat` command for the current task. On Ethan's selected Dvorak - QWERTY Cmd input source, Command+Option reaches semantic S through macOS key code 41. Preserve the user's existing shortcut map. Steer Queued Message sends Codex's built-in Command-Return shortcut as one bounded process-targeted key cycle; never route Steer through Accessibility-tree discovery.
- Edit Queued Message remains the queued-row Accessibility exception. Codex's Chromium Accessibility bridge flattens queued-row wrapper groups, so anchor Edit to the three simultaneously visible exact controls in one window and one left-to-right line: `Steer`, `Delete queued message`, then `Queued message actions`. De-duplicate Accessibility traversal so wrapper aliases cannot exhaust the bounded search. Snapshot each focused-window element's role, labels, frame, enabled state, and actions once rather than issuing repeated cross-process reads. Reject clipped controls, ties, controls outside the focused window, and broad-label matches. Open the visually highest unambiguous validated `Queued message actions` button, then search the focused window with the same 6,000-element budget and every other top-level window with its own 1,500-element budget for all exact enabled pressable visible `Edit message` items. Prefer elements absent from the pre-open snapshot, fail on multiple candidates, and never let a large conversation tree starve its own web popover. Yield the main run loop between bounded attempts instead of sleeping it, but re-check the session/permission gates after every yield and before every AX press. Keep only one Edit journey in flight; cancel its generation on lock, sleep, mode exit, or teardown. Require `AXPress`, log privacy-safe stage diagnostics, and call Edit unconfirmed until an authoritative queued-row state change can be observed. Do not blindly re-press the actions menu when no exact menu item was ever observed, and do not substitute Codex's `openSideChat` command for opening the newest queued message in side chat: the current Codex command opens the current task.
- Codex AX completion rules refine the two entries above. Voice must use the same window scope, timeout, element cap, and wall-clock budget before and after dispatch; a capped, cancelled, or incomplete scan is unknown, never inactive. Edit must de-duplicate nodes when enqueueing, require its initial anchor traversal to complete, read only role and exact relevant labels before frame/enabled/actions, reserve a separate popup deadline, and preserve the original source owner across rejected nested presses. These completion rules supersede the earlier eager-snapshot wording.
- Claude uses one specialized app-specific definition for both automatic and manually selected journeys. Preserve app-child Exit on cells 2 and 10. Match the Codex muscle-memory positions only where Claude exposes a genuine equivalent: Voice Mode cell 4, New Chat cell 5, Mute/Unmute Voice Mic cell 6, and Enter cell 7. Claude-specific verified actions use Settings cell 1, Search cell 3, Reload cell 8, Toggle Sidebar cell 9, Previous Tab cell 11, and Next Tab cell 12. Use Claude's observed native menu accelerators for keyboard actions and exact enabled pressable Accessibility labels for Search/Sidebar/Voice/Mute. Set a bounded AX messaging timeout, traverse only Claude's focused window with identity de-duplication and an index-based queue, and fail closed on broad labels or multiple matches. Never copy a Codex command name or keybinding into Claude merely because the card position matches.
- Chrome cell 1 closes the current Chrome tab with Command-W, while Chrome cell 8 deliberately duplicates New tab with Command-T and must not close the current Chrome window. VS Code cell 1 closes the current editor tab with Command-W, cell 7 opens the Command Palette with one bounded Command-Shift-P cycle, and cell 11 sends VS Code's native F12 `editor.action.revealDefinition` shortcut; do not duplicate the Default map's Back/Forward pair inside the VS Code child. VS Code cell 4 alternates the integrated terminal through the bundled VS Code Bridge: the first action in each extension-host session must call `workbench.action.closePanel`, the second must call `workbench.action.terminal.focus`, and later presses repeat Hide then Show. Never substitute Control-` because VS Code focuses an already-visible unfocused Terminal before it hides it, and never use Command-J because it toggles the generic panel. VS Code cell 12 sends one app-targeted Control-C interrupt; Terminal and iTerm use that same app-specific cell and shortcut. Resolve semantic `C` from the active keyboard layout at dispatch time and fail closed if it cannot be resolved. Never hard-code physical QWERTY-C key code 8: Control does not activate Ethan's `DVORAK - QWERTY CMD` Command remap, so that position resolves to `J` and Control-J enters a newline instead of interrupting. Preserve bounded PID-targeted delivery rather than leaking a global shortcut. Cells 5 and 8 use the same 300 ms Previous/Stage + Previous and Next/Stage + Next gestures as the ordinary VS Code layer; cell 9 uses the same classifier for Stage + Next single / exact Undo Stage double, while physical cell 6 owns Cursor History + Wheel. Cursor History and Terminal visibility use the bundled allow-listed Agentic Mouse VS Code Bridge and VS Code's direct command API; they have no synthetic keyboard fallback and must leave Ethan's keybindings file byte-identical. Keep top-level cell 6 as the global YouTube scrub wheel action even while VS Code is frontmost. Source the VS Code page cells, HUD copy, and commands from `VSCodeModeAction` instead of duplicating a second app-mode map. Keep every unimplemented card honestly Spare; never activate a target app to simulate background delivery. (Codex task: 01a039f7-873c-7c30-b3dc-af8a6724ace5)
- Automatic frontmost-app mode and the manual Choose App journey must resolve every configured app through the same `AppSpecificTarget.definition`. They may differ only in targeting lifetime: automatic follows focus, while manual stays locked. Never duplicate, copy, or independently edit an app's card array for one journey; a card added to an app definition must appear identically in both. Keep ordinary data-driven starter pages in the `StandardAppMode` registry and layer any stateful held-wheel control through the shared `WheelChordControl` resolver; keep other specialized stateful/gesture pages in their dedicated action types. Prefer exact installed-app menu accelerators and current official app documentation, add only fixed non-destructive shortcuts, reserve both cell 2 and universal cell 10 as Exit on every app child, and leave a maintenance app's remaining cells honestly Spare when it exposes no useful safe command.
- Automatic frontmost-app mode recognizes iPhone Mirroring by exact bundle ID `com.apple.ScreenContinuity`; do not add it to the full manual Choose App grid unless Ethan explicitly chooses what it should replace. Its child cell 1 is `Notifications` (Corsair 1 / Razer 3) and sends Apple's supported global Fn-N shortcut only while iPhone Mirroring is frontmost, the session is unlocked, and Accessibility is trusted. Resolve semantic `N` from the current keyboard layout at dispatch time; never hard-code physical QWERTY-N because Fn does not activate Ethan's `DVORAK - QWERTY CMD` Command remap. Fail closed if the semantic key cannot be resolved. Emit the real four-event Globe/Fn lifecycle (`flagsChanged` down, N down/up, `flagsChanged` up); setting `maskSecondaryFn` only on N is not a hardware-shaped shortcut. This opens macOS Notification Center, where iPhone Mirroring delivers iPhone notifications. Observe the public on-screen Notification Center window transition and report confirmed open/close only from that state change; event creation or posting alone is never confirmation. Do not fake an unavailable iPhone pull-down gesture, use UI automation, or weaken app-child exits on cells 2 and 10.
- Default physical cell 6 is always `YouTube Scrub + Wheel`. Each exact-device Karabiner base sends a strict source-specific press/release lifecycle to Agentic Mouse. Release with no wheel input posts one exact `-5` VoiceInk bridge notification; any observed wheel input suppresses that click action, including a filtered duplicate, phased pass-through, ambiguous two-mouse event, or failed output. Wheel up posts the exact `+5` notification and wheel down posts the exact `-5` notification, with one output per reconstructed physical ratchet and no Chrome focus. Teardown clears the hold without producing a click. Utility physical cell 8 owns `Intelligence on demand` and emits one complete hardware-shaped left-Option + Space lifecycle only after the unlocked-session and Accessibility gates pass. Keep any same-cell child-mode action scoped to that deliberately entered mode.
- Keys mode opens directly from top-level physical cell 9. Corsair physical cells 5/4/7/1 emit Up/Down/Right/Left; on the left-handed Razer, cells 1 and 7 deliberately swap the horizontal meanings so the physical gestures are mirrored. Physical cell 3 emits Undo as Command-Z, cell 6 enters Keypad, cell 9 emits Next Track, cell 8 emits Space, cell 11 emits Backspace, and cell 12 emits Enter. Cell 2 is spare. The optional device-local password belongs to Utility cell 7 and must never use the clipboard, configuration files, generated rules, or logs; it must fail closed when locked, untrusted, or unconfigured. Press universal physical cell 10 to exit. Emit keys and the consumer-media event natively in the generated exact-device Karabiner layer, keep every output non-repeating, let Agentic Mouse update only HUD/lighting state, and restore the ordinary map immediately on exit or lease failure.
- Keypad character delivery must work in all editable foreground inputs, including `AXSecureTextField`, and when a Chromium/custom control hides `AXFocusedUIElement`. Preserve an exact AX element anchor when one is exposed; otherwise anchor to the unchanged frontmost application PID, and re-resolve before every irreversible event. Insert committed characters directly as process-targeted UTF-16 `CGEvent`s; Keypad must never read a field value, mutate the pasteboard, or synthesize Command-V. Backspace and Return remain process-targeted native key events. A frontmost-app change cancels pending text.
- Keep the reusable mode legend at 75% of its former 940-point design (705-point width with proportionally scaled typography, spacing, padding, corners, and strokes). It has no large header or instructional subtext. Every Default, Utility, Keys, Keypad, app-selector, Codex, Chrome, VS Code, unsupported-app, and future mode card contains only its concise action name plus one small printed button label for the exact source mouse. Never add an explanatory subtitle, implementation detail, shortcut chord, spare explanation, or second prose line to a mode card; keep errors and verified action feedback in the dedicated banner/footer instead. The reusable `ModeHUDLegendItem` contract must not expose a detail/subtitle field. Never show both devices on the same card. Use one compact bottom row for the mode title and active mouse.
- Use macOS 26's native `NSGlassEffectView` only for the legend's single outer panel background, with `.clear` style and nil tint; use `NSVisualEffectView` as the older-macOS fallback. Keep cards as ordinary opaque SwiftUI fills above that one glass surface: ordinary action-family fills are deliberately calmer, while destination-mode cards remain fully saturated. Never nest native glass inside cards or replace their semantic action colours. The HUD is deliberately non-activating and click-through, so preserve native environmental lensing without faking interactive deformation or accepting pointer input. App identity artwork belongs only inside a card that names a concrete app: the top-level current-app slot prefers the exact running `.app` path, while named `Choose app` slots use their canonical configured bundle identifiers. Resolve the real installed icon dynamically and render one enlarged, moderately blurred copy edge-to-edge inside that card behind its white label; keep the app recognisable instead of smearing it into an abstract wash. Derive the app mode's overall identity accent from the strongest populated chromatic cluster in a tiny icon raster instead of a muddy raw average; normalize it to a strong legible mode colour, use bounded grey for a genuinely achromatic icon, and retain the deterministic existing accent if sampling fails. Apply that accent to the app-mode panel/perimeter, destination card, and mouse mode lighting without replacing semantic action-family fills. Share one in-memory cache keyed by resolved `.app` path across both source HUDs and automatic/manual app journeys; never persist icons or derived colours. Never apply app artwork to the whole panel, the child app page's action grid, selector spares, or unrelated cards; never copy or persist icon bytes, add a separate pastel panel gradient, or whiten the cards into a competing visual system.
- On the neutral Default page, render every card that opens Keys, the current app mode, Utility, or another submenu with the destination mode's exact saturated accent as a fully opaque fill and border. Keep ordinary Default actions neutral/translucent. Apply this through `destinationModeAccent`, never by matching titles or device-specific printed numbers.
- Show the packaged Agentic Mouse marketing version and exact build number in every legend footer. Read both from the running app bundle, never hard-code them in the view. Every successfully signed candidate intended for local installation must advance both identifiers exactly once after packaging and signature verification: increment the patch component of `CFBundleShortVersionString` by default and increment the positive-integer `CFBundleVersion`. A named major/minor release may supply a higher explicit marketing version, but no install may reuse or lower the recorded marketing version. Failed candidates and ordinary ad-hoc development builds must consume neither identifier. Persist both resulting source values as the installed-build reference.
- Default text entry to buffered pending-character commit, anchored to the frontmost PID and focused AX element. A focus/element change cancels pending text. Never use an unanchored global backspace-and-replace loop as the safe default.
- Runtime mode colours are temporary output only. While Agentic Mouse is running, maximum solid white is the baseline on both mice whenever no explicit mode or alert is active; mode colours override it and exit returns to white. The Scimitar exposes only logo plus the complete thumb-grid zone; the Naga exposes wheel, logo, plus the complete thumb-grid zone. Use the logo/wheel for the current mode colour and the whole grid for the last-action accent; never claim twelve independent LED buttons. A true app teardown or sleep still releases the Corsair shared layer and restores Razer Spectrum so vendor ownership is never stranded. Never present this as a Mural, profile, or device-memory editor.
- Keep Razer USB presence and Razer lighting ownership recoverable while Agentic Mouse remains alive. Poll exact `1532:008d` presence read-only at a bounded two-second interval, emit work only on a real transition, drop a stale handle without sending Spectrum to an absent device, and retry the accepted idle-white `NOSTORE` frame until the reconnected device acknowledges it. Stable connected polls must never repaint. Input recovery through Karabiner does not prove the independent lighting transport recovered.
- Do not store credentials, application keys, tokens, bridge addresses, device IDs, serials, generated app bundles, or downloaded vendor binaries in Git.
- Keep each Codex command on its proven transport instead of treating every Electron accelerator alike. New Chat remains an ordinary Command-N sent directly to the running ChatGPT PID. Open Side Chat requires ChatGPT frontmost and uses macOS System Events for its exact Command-Option-S accelerator. Realtime Voice also requires ChatGPT frontmost, but its Control-Shift-V OS-global/Carbon accelerator must use the ANSI V key and one complete hardware-shaped modifier lifecycle through the shared system event source. Never replace these routes with a broad Accessibility press or a flag-only Quartz chord, and fail closed if a foreground-only action loses ChatGPT focus.
- Keep hardware, event-source, text-output, state-machine, and UI boundaries injectable so core behavior can be tested without a connected mouse, Accessibility permission, or iCUE.
- Chrome physical cell 1 (Corsair printed 1 / Razer printed 3) is `Close current tab`: send Command-W directly to the running Chrome process. Chrome physical cell 8 (Corsair printed 8 / Razer printed 8) deliberately duplicates `New tab`: send Command-T and never close the current Chrome window from this control. Chrome physical cell 7 (Corsair printed 7 / Razer printed 9) is `Hold 2× speed`. An ordinary physical press asks the existing VoiceInk YouTube Bridge to set the currently playing, extension-selected YouTube video to 2×, and ordinary release restores that exact video's previous playback rate. A same-source double-click locks the same opaque-token lease at 2× after release; the next same-source double-click explicitly resets that bound video to 1×. Never combine taps from different mice. Renew the short lease while held or locked so a lost release cannot strand the video. Mode exit by the lock-owning mouse, lock, sleep, helper disconnect, extension reload, or app teardown must fail safe to the saved prior rate; only the deliberate second double-click may request 1×. Reuse the bridge's PiP → last-focused active → audible → active → playback-recency targeting; never focus Chrome, change play/pause state, guess a tab in Agentic Mouse, or describe notification dispatch as confirmed playback state. Chrome physical cell 3 (Corsair printed 3 / Razer printed 1) is `Open DevTools`: send Chrome's documented Command-Option-I shortcut directly to the running Chrome process on press only. Chrome physical cell 4 (Corsair printed 4 / mirrored Razer printed 6) holds the Chrome Tabs wheel chord: one wheel-up detent sends Command-Option-Left for the previous tab and one wheel-down detent sends Command-Option-Right for the next tab. Keep this Chrome-only polarity independent from every other wheel family. Chrome physical cell 5 is also `New tab`: send one bounded Command-T cycle directly to the running Chrome process on press only. Chrome physical cell 6 (Corsair printed 6 / Razer printed 4) is `Reload current tab`: send one bounded Command-R cycle directly to the running Chrome process on press only. Use all controls in automatic and manually selected Chrome mode, and leave unarmed scrolling untouched. Chrome mode must not duplicate Default-mode Forward/Back or synthesize layout-sensitive Command-Bracket history shortcuts; ordinary history navigation remains owned by the Default base map.
- Keep Safari's app-specific grid aligned with Chrome wherever both browsers expose the same action: cell 1 `Close tab`, cell 3 `Open DevTools`, cells 5 and 8 `New tab`, cell 6 `Reload`, cell 11 `Reopen tab`, and cell 12 `Find page`. Safari keeps its separate tab-direction controls on cells 4/7 and Forward on cell 9; do not restore Back on cell 8 because Ethan deliberately chose the same New Tab duplicate as Chrome. Cells 2 and 10 remain Exit. `Open DevTools` sends Safari's native Option-Command-I Web Inspector shortcut directly to the running Safari process on press only and replaces the lower-value Downloads action. Automatic frontmost Safari mode and manually selected Safari mode must resolve the same `StandardAppMode` definition; do not add a Safari-only physical transport, activate Safari, or route browser chrome through a page extension.
- Codex physical cell 9 (Corsair printed 9 / mirrored Razer printed 7) is `Open side chat`. Send Codex's built-in Command-Option-S `openSideChat` app accelerator through macOS System Events only while ChatGPT is frontmost; with Ethan's Dvorak - QWERTY Cmd layout, send physical key code 41 rather than QWERTY-S key code 1. It opens the current task as a side task. Do not use Quartz or `AXPress`, mislabel the command as opening a queued message, alter the user's normal shortcut, or reintroduce the rejected `New chat + pin` sequence.
- Ethan gives standing authorization to stage, commit, and push every completed Agentic Mouse source change to this repository's current branch without asking him to review or stage it. After the full gate and live completion, automatically stage every safe task-owned repository change, commit it, push it to `origin`, and verify the remote commit. Exclude credentials, application keys, tokens, bridge addresses, device identifiers, live Karabiner profiles, `Rollbacks/`, generated app bundles, downloaded vendor binaries, and other local/private artifacts from the public repository. Do not absorb ambiguous unrelated or incomplete work; reconcile ownership first and stop only when it cannot be separated safely. This standing publication authority does not authorize a LaunchAgent, Accessibility changes, unrelated hardware/settings, iCUE mutation, or device firmware changes.
- Run focused tests and a clean build before declaring implementation complete. Record any untestable hardware boundary honestly.
