// Source-tagged F17 captures the reviewed file before moving; a late stage consumes that identity, not the new current file.
{
  "id": "vscode-go-to-previous-change",
  "title": "VS Code: go to previous change",
  "description": "Press marks this mouse's Previous button held; unused release emits source-tagged F17, then opens one second to stage the pre-navigation file. A consumed hold clears on release.",
  "manipulator": {
    "conditions": [
      { "type": "frontmost_application_if", "bundle_identifiers": ["^com\\.microsoft\\.VSCode(?:Insiders)?$"] }
    ],
    "to": [
      { "set_variable": { "name": "$binding_variable", "value": 1 } }
    ],
    "to_after_key_up": [
      { "key_code": "f17", "modifiers": "$binding_output_modifiers", "repeat": false, "conditions": [{ "type": "variable_if", "name": "$binding_variable", "value": 1 }] },
      { "set_variable": { "name": "$binding_variable", "expression": "system.now.milliseconds + 1000" }, "conditions": [{ "type": "variable_if", "name": "$binding_variable", "value": 1 }] },
      { "set_variable": { "name": "$binding_variable", "value": 0 }, "conditions": [{ "type": "variable_if", "name": "$binding_variable", "value": 2 }] }
    ]
  }
}
