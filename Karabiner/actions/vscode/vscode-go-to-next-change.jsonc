// Source-tagged F13 captures the reviewed file before moving; a late stage consumes that identity, not the new current file.
{
  "id": "vscode-go-to-next-change",
  "title": "VS Code: go to next change",
  "description": "Press marks the exact mouse's Next button held; release emits F13 only when the same hold was not consumed, then starts that mouse's one-second Enter dead zone.",
  "manipulator": {
    "conditions": [
      { "type": "frontmost_application_if", "bundle_identifiers": ["^com\\.microsoft\\.VSCode(?:Insiders)?$"] }
    ],
    "to": [
      { "set_variable": { "name": "$binding_variable", "value": 1 } }
    ],
    "to_after_key_up": [
      { "key_code": "f13", "modifiers": "$binding_output_modifiers", "repeat": false, "conditions": [{ "type": "variable_if", "name": "$binding_variable", "value": 1 }] },
      { "set_variable": { "name": "$binding_variable", "expression": "system.now.milliseconds + 1000" }, "conditions": [{ "type": "variable_if", "name": "$binding_variable", "value": 1 }] },
      { "set_variable": { "name": "$binding_variable", "value": 0 }, "conditions": [{ "type": "variable_if", "name": "$binding_variable", "value": 2 }] }
    ]
  }
}
